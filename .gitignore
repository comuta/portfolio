__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/
.DS_Store
instance/
.env

# Private site config with real personal/legal data (name, address, email).
# See content/site.config.example.json for the placeholder template.
content/site.config.json

# Docker Compose runtime content directory (mirrors /var/lib/portfolio).
# It's its own git repo (FA-41) managed by the admin app, not this repo.
/data/
