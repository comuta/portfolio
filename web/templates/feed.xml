<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site_config.name if site_config else "Portfolio" }} — Projekte</title>
  <id>{{ request.host_url }}</id>
  <link href="{{ request.host_url }}"/>
  <link href="{{ request.url }}" rel="self"/>
  <updated>{{ (projects[0].datum.isoformat() ~ "T00:00:00Z") if projects else "1970-01-01T00:00:00Z" }}</updated>
  {% for project in projects %}
  <entry>
    <title>{{ project.titel }}</title>
    <id>{{ request.host_url }}projekte/{{ project.slug }}/</id>
    <link href="{{ request.host_url }}projekte/{{ project.slug }}/"/>
    <updated>{{ project.datum.isoformat() }}T00:00:00Z</updated>
    <summary>{{ project.kurzbeschreibung }}</summary>
  </entry>
  {% endfor %}
</feed>
